# 贡献指南

感谢你愿意为这个清单出力。为了让仓库长期保持可用，请先读完「收录边界」一节 —— 这是最容易出问题的地方。

## 只改一个文件

**所有内容修改都只发生在 `data/papers.yaml`。**

`README.md`、`papers_by_section/`、`papers_by_surface/` 全部是 GitHub Actions 的自动生成产物，直接编辑它们会在下次构建时被覆盖。

章节结构调整改 `data/sections.yaml`。

## 收录边界

本仓库只收录**以 LLM 推理 / 训练基础设施本身为主要研究对象**的安全工作。

以下三条是实测中最容易失守的边界，请逐条自查。

### 边界 1：Security-of-AI，而不是 AI-for-Security

| 收 | 不收 |
|---|---|
| 攻击或防护 LLM serving 系统 | 用 LLM/MoE 做恶意软件分类 |
| 分布式训练的完整性保障 | 用 agent 做渗透测试、漏洞挖掘 |
| GPU 侧信道泄露模型资产 | 用 AI 辅助 CTF |

这两个领域名字相近但完全不同。检索时 `MoE × 安全词` 会召回大量「用 MoE 架构做安全分类任务」的论文，它们不属于本仓库。

### 边界 2：Infra 层，而不是纯模型层

论文必须涉及 **serving / training 系统的状态、资源或拓扑**。

判据是问一句：**如果把这篇工作的系统部分抽掉，结论还成立吗？**

- 如果成立 → 说明它本质是模型层工作，不收
- 如果不成立（结论依赖 KV 布局、调度行为、专家并行拓扑、量化部署路径等）→ 收

这条在第 3 章「Infra 优化引发的 Safety 漂移」最难判。参考：
- ✓ 收：KV cache 量化导致对齐崩塌 —— 结论依赖具体的缓存量化机制
- ✗ 不收：讨论 RLHF 对齐脆弱性的一般性研究 —— 与部署形态无关

### 边界 3：作用点必须在基础设施上

不收「把通用越狱换个部署环境重跑一遍」的工作。

第 2.3 节「MoE 路由操纵」尤其要注意：论文的攻击手段必须作用于**路由机制本身**（如操纵 gate、诱导专家选择），而不是用一个普通越狱 prompt 去打 MoE 模型然后声称这是 MoE 安全研究。

### 拿不准怎么办

**提交 PR，并在描述里说明你的判断理由。** 边界情况由维护者定夺，但请不要因为拿不准就默默不提 —— 漏收比误收更难发现。

## 条目格式

```yaml
  - id: "2608.09225"                    # arXiv ID，不带版本号
    title: "官方完整标题"                 # 必须与 arXiv 返回的标题精确一致
    abbr: "方法缩写"                      # 可选
    date: "2026-08"                      # arXiv v1 提交年月，不是 v2/v3
    venue: "arXiv"                       # 或 NDSS 2026 / CCS 2025 / OSDI 2026 等
    section: "1.1"                       # 叶子小节 ID，见 data/sections.yaml
    boundary: [cross-tenant, cross-request]   # 信任边界，可多选
    summary: >-
      中文简介，2-4 句、150-300 字，须含动机 / 机制 / 关键数字。
    code: "https://github.com/..."       # 可选
```

非 arXiv 来源（会议论文无预印本、CVE、厂商公告、官方文档）用 `url` 字段替代 `id`：

```yaml
  - url: "https://www.ndss-symposium.org/ndss-paper/..."
    title: "..."
    venue: "NDSS 2025"
```

### 几条硬性要求

**标题必须与 arXiv 返回值精确一致。** CI 会做标题相似度比对（阈值 0.92）并叠加长度差检查。不要手动截短标题 —— 建仓时就因为漏掉副标题被 CI 拦下过一次。

**date 以 arXiv v1 为准。** 用 `sortBy=submittedDate` 检索时返回的是 v1 日期，但如果你从网页复制，很容易拿到 v2/v3 的更新月份。CI 会校验这一项。

**简介要有具体数字。** 写「攻击成功率从 7.4% 抬升到 64.9%」，而不是「效果显著」。关键数字是读者判断一篇工作值不值得读的主要依据。

**簡介写动机、机制、结论三件事。** 不要只翻译摘要第一句。

**信任边界要标全。** 一篇工作跨越多层边界就都标上 —— 这个字段决定了它出现在哪些 `papers_by_surface/` 视图里，也决定了交叉矩阵的准确性。

## 本地验证

提交前请跑这两个脚本：

```bash
pip install pyyaml

python3 scripts/build.py            # 结构校验 + 生成产物
python3 scripts/check_links.py      # arXiv 元数据校验（全量，较慢）
python3 scripts/check_links.py --sample 8   # 抽样快速验
```

`build.py` 会检查必填字段、ID 格式、重复条目、章节归属、锚点有效性。
`check_links.py` 会检查 arXiv 可达性、标题精确匹配、v1 日期一致性。

两个都通过再提 PR。CI 会在 PR 上重跑一遍（元数据校验抽样 12 条），每周一全量跑。

## 检索新论文

```bash
python3 scripts/fetch.py --start 2026-09-05 --end 2026-09-11
```

脚本按「系统级形态词 × 系统安全词 × 章节分类键」三层交集召回，并自动与现有条目去重。

**注意：召回结果必须人工筛。** 本方向检索噪音很大，宽口径实测精度只有约 10%。脚本头部注释详细记录了口径设计与四个已踩过的坑（灌水词、跨域同名词、主分类限定、精度抽样），修改检索词前请先读。

## PR 描述建议包含

- 本次新增条目的分节分布
- 检索窗口与召回数
- **按范围剔除的论文及理由** —— 这个最有价值，能帮助后续维护者校准边界
- 归类上的边界情况与你的判断依据
